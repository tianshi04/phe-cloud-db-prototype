[package]
name = "paillier-crypto"
version = "0.1.0"
edition = "2021"

[dependencies]
num-bigint = { version = "0.4", features = ["serde", "rand"] }
num-traits = "0.2"
num-integer = "0.1"
rand = "0.8"
serde = { version = "1.0", features = ["derive"] }

[lints]
workspace = true
